# Copilot Instructions

- When implementing new functionality, add or update unit tests that cover the new behavior.
- Prefer placing unit tests in the `UsbFileSync.Tests` project unless there is a clear reason to use a different test location.
- When implementing or changing a user-facing feature, add or update the documentation in `README.md` so the documented feature set stays current.
