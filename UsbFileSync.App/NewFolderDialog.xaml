<Window x:Class="UsbFileSync.App.NewFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Folder"
        Width="420"
        Height="170"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F3F3F3">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Enter the name of the new folder."
                   FontSize="14"
                   FontWeight="SemiBold" />

        <TextBox x:Name="FolderNameTextBox"
                 Grid.Row="1"
                 Margin="0,12,0,0"
                 VerticalContentAlignment="Center" />

        <StackPanel Grid.Row="2"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="90" Margin="0,0,10,0" Content="Cancel" IsCancel="True" />
            <Button Width="90" Content="Create" IsDefault="True" Click="OnCreateClicked" />
        </StackPanel>
    </Grid>
</Window>